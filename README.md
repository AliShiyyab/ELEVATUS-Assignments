# ELEVATUS-Assignments

> ##Installations:

> git clone <repo_link>

> python3 -m venv venv_name

> pip install -r requirements.txt 

> ran application using `docer-compose up --build`

This is a simple application wrote by Python programming language, and I use a fastAPI framework.